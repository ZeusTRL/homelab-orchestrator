# Core web framework
fastapi==0.115.0
uvicorn==0.31.1

# Database + ORM
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3  # <-- new line, replaces psycopg2

# Networking / SSH / Scanning
paramiko==3.4.0
python-nmap==0.7.1

# File export / packaging
requests==2.32.3
zipfile36==0.1.3

# SNMP + LLDP polling (asyncore removed, 3.12-compatible)
pysnmp==6.2.6
pyasn1==0.6.0
pyasn1-modules==0.4.1

# Utilities
networkx==3.3
redis==5.0.8
rq==1.16.2

# Optional extras for config templating
jinja2==3.1.4
